# SPA fallback: client-side routes (/trading/journal, /trading/notes/...)
# must serve the app shell, not 404.
RewriteEngine On
RewriteBase /trading/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /trading/index.html [L]
