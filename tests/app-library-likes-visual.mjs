// Library video likes (manual): like, count on the card, unlike, and who can read or forge likes.
//   node tests/app-library-likes-visual.mjs   (APP_URL / OUT / THEME env, PHONE=1 for 390x844)
import { createRequire } from "module";
import { existsSync, mkdirSync, readFileSync } from "fs";
import { homedir, tmpdir } from "os";
import { join } from "path";
const require = createRequire(import.meta.url);
const PW_PATHS = ["C:/Users/Deb/Desktop/Projects/outback-running-club/client/node_modules/playwright", "C:/Users/clari/OneDrive/Desktop/Projects/clients/outback-running-club/client/node_modules/playwright"];
const { chromium } = require(PW_PATHS.find((p) => existsSync(p)) || "playwright");
const OUT = process.env.OUT || join(tmpdir(), "app-library-likes"); mkdirSync(OUT, { recursive: true });
const REF = "cqdignbleethroyxxvzr", SB = `https://${REF}.supabase.co`;
const mgmt = readFileSync(join(homedir(), ".supabase", "access-token"), "utf8").trim();
const keys = await (await fetch(`https://api.supabase.com/v1/projects/${REF}/api-keys?reveal=true`, { headers: { Authorization: `Bearer ${mgmt}` } })).json();
const service = keys.find((k) => k.name === "service_role").api_key, anon = keys.find((k) => k.name === "anon").api_key;
const link = await (await fetch(`${SB}/auth/v1/admin/generate_link`, { method: "POST", headers: { apikey: service, Authorization: `Bearer ${service}`, "Content-Type": "application/json" }, body: JSON.stringify({ type: "magiclink", email: "appreview@d1fpc3.com" }) })).json();
const session = await (await fetch(`${SB}/auth/v1/verify`, { method: "POST", headers: { apikey: anon, "Content-Type": "application/json" }, body: JSON.stringify({ type: "magiclink", token_hash: link.hashed_token }) })).json();
if (!session.access_token) throw new Error("verify failed");
const browser = await chromium.launch();
const PHONE = process.env.PHONE === "1", PRE = PHONE ? "p-" : "d-"; const ctx = await browser.newContext(PHONE ? { viewport: { width: 390, height: 844 }, deviceScaleFactor: 2, isMobile: true, hasTouch: true } : { viewport: { width: Number(process.env.W || 1440), height: Number(process.env.HGT || 900) } });
await ctx.addInitScript(([k, v, theme]) => { localStorage.setItem(k, v); if (theme) localStorage.setItem("echelon-theme", theme); localStorage.setItem("echelon-gex-tour", "1"); localStorage.setItem("echelon-quotes-off", "1"); localStorage.setItem("echelon-splash-day", new Date().toDateString()); localStorage.setItem("echelon-chart-tf", "5m"); }, [`sb-${REF}-auth-token`, JSON.stringify(session), process.env.THEME || ""]);
const page = await ctx.newPage();
page.on("pageerror", (e) => console.log("PAGEERROR", e.message));
await page.goto(process.env.APP_URL || "http://127.0.0.1:8123/echelon/app/", { waitUntil: "domcontentloaded" });
await page.waitForSelector("#td-h1", { timeout: 30000 });
await page.waitForTimeout(1500);
const shot = (n) => page.screenshot({ path: `${OUT}/${n}.png` });
const go = async (v) => { await page.evaluate((v) => document.querySelector('.tab[data-view="' + v + '"]')?.click(), v); await page.waitForTimeout(2200) };
const fails = []; const ok = (c, w) => { console.log((c ? "ok   " : "FAIL ") + w); if (!c) fails.push(w) };
const H = { apikey: service, Authorization: `Bearer ${service}`, "Content-Type": "application/json", Prefer: "return=representation" };
const me = session.user.id;
const vids = await (await fetch(`${SB}/rest/v1/library_videos?is_published=eq.true&select=id,title&order=created_at.desc&limit=1`, { headers: H })).json();
const vid = vids[0]; ok(!!vid, "a published video exists: " + vid?.title);
await fetch(`${SB}/rest/v1/library_likes?video_id=eq.${vid.id}&user_id=eq.${me}`, { method: "DELETE", headers: H });
// someone else already likes it: a throwaway member
const bot = await (await fetch(`${SB}/auth/v1/admin/users`, { method: "POST", headers: H, body: JSON.stringify({ email: `harness-liker-${Date.now()}@d1fpc3.test`, password: "x-" + Math.random().toString(36).slice(2), email_confirm: true }) })).json();
await fetch(`${SB}/rest/v1/library_likes`, { method: "POST", headers: H, body: JSON.stringify({ video_id: vid.id, user_id: bot.id }) });
const before = (await (await fetch(`${SB}/rest/v1/library_likes?video_id=eq.${vid.id}&select=user_id`, { headers: H })).json()).length;

await go("library"); await page.waitForTimeout(1200);
const card = page.locator(".lib-card", { hasText: vid.title }).first();
ok((await card.locator(".lk").textContent()).trim() === String(before), "the card shows the like count: " + before);
await card.click(); await page.waitForTimeout(2500);
ok((await page.textContent("#lib-like-n")) === String(before) && !(await page.locator("#lib-like.on").count()), "the player shows the count, not yet liked by me");
await shot(PRE + "like-before");
await page.click("#lib-like"); await page.waitForTimeout(1500);
ok(await page.locator("#lib-like.on").count() === 1 && (await page.textContent("#lib-like-n")) === String(before + 1), "tapping the heart likes it and the count goes up");
let mine = await (await fetch(`${SB}/rest/v1/library_likes?video_id=eq.${vid.id}&user_id=eq.${me}&select=video_id`, { headers: H })).json();
ok(mine.length === 1, "the like is saved");
await shot(PRE + "like-on");
await page.click("#lib-x"); await page.waitForTimeout(700);
ok(await card.locator(".lk.me").count() === 1 && (await card.locator(".lk").textContent()).trim() === String(before + 1), "the card shows my like");
await shot(PRE + "like-card");
await page.reload({ waitUntil: "domcontentloaded" }); await page.waitForSelector("#td-h1", { timeout: 30000 }); await page.waitForTimeout(2500); await go("library"); await page.waitForTimeout(1200);
ok(await page.locator(".lib-card", { hasText: vid.title }).first().locator(".lk.me").count() === 1, "it survives a reload");
await page.locator(".lib-card", { hasText: vid.title }).first().click(); await page.waitForTimeout(2200);
await page.click("#lib-like"); await page.waitForTimeout(1500);
mine = await (await fetch(`${SB}/rest/v1/library_likes?video_id=eq.${vid.id}&user_id=eq.${me}&select=video_id`, { headers: H })).json();
ok(mine.length === 0 && !(await page.locator("#lib-like.on").count()) && (await page.textContent("#lib-like-n")) === String(before), "tapping again takes it back");
// a member cannot like as someone else; a signed-in non-member sees nothing
const forge = await fetch(`${SB}/rest/v1/library_likes`, { method: "POST", headers: { apikey: anon, Authorization: `Bearer ${session.access_token}`, "Content-Type": "application/json" }, body: JSON.stringify({ video_id: vid.id, user_id: bot.id + "" }) });
ok(forge.status >= 400, "a member cannot add a like under another member's name: " + forge.status);
const bl = await (await fetch(`${SB}/auth/v1/admin/generate_link`, { method: "POST", headers: H, body: JSON.stringify({ type: "magiclink", email: bot.email }) })).json();
const bs = await (await fetch(`${SB}/auth/v1/verify`, { method: "POST", headers: { apikey: anon, "Content-Type": "application/json" }, body: JSON.stringify({ type: "magiclink", token_hash: bl.hashed_token }) })).json();
const peek = await (await fetch(`${SB}/rest/v1/library_likes?select=video_id`, { headers: { apikey: anon, Authorization: `Bearer ${bs.access_token}` } })).json();
ok(Array.isArray(peek) && peek.length === 0, "an account that owns nothing reads zero likes: " + JSON.stringify(peek).slice(0, 60));
await fetch(`${SB}/auth/v1/admin/users/${bot.id}`, { method: "DELETE", headers: H });
await browser.close();
if (fails.length) { console.log(["FAILS", ...fails].join(String.fromCharCode(10) + " - ")); process.exit(1) } else console.log("ALL OK");
